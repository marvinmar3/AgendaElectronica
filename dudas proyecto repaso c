dudas proyecto repaso c
-- no sd pudo utilizar el set %[^\n], utilicé fgets

getchar();

if(posicion != -1) que significa -1


strcmp no compara mayusculas -> no puedo utilizarlo 

-- falta agregar memoria dinamica y manejo de archivos

fgets (argumentos- investigar stdin)
agregar nombre: ap: etc, 
agregar desc, fecha y hora para la salida 

cambiar la busqueda de nombre por posicion, igual en actualizar



tarea
buscar por fecha limite o 
buscar por prioridad

agregar el salto de linea en actualizar contacto
corregir encontrado

recursion -> mostrar contactos

comparar_cadenas -> buscar y actualizar

limpieza en citas y tareas

if (agenda == NULL) {
        printf("Error al asignar memoria\n");
        exit(1); -cambie por return -1
    }